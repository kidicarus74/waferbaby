- unless @scrawls.length < 1
  - throw_content :atom_feed, "/wallscrawl"
  %h2
    Scrawls
    - if params["created_year"]
      = "from #{date_title}"
    = " (" + link_to("Add", resource(:scrawls, :new)) + ")"
  - @scrawls.each do |@scrawl|
    = partial :scrawl
- else
  %h2 Aw, I'm sorry!
  %p There are no scrawls here.